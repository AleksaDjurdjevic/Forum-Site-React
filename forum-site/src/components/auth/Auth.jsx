import React, { useState } from 'react';
import { Outlet } from 'react-router-dom'
import './Auth.scss'

const Auth = () => {
    //html template
    return (
        <Outlet />
    );
}

export default Auth;
